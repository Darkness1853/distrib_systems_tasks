events {}

http {
    server {
        listen 80;

        location /api/tasks/ {
            proxy_pass http://tasks-svc-s17:8196/api/tasks/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}